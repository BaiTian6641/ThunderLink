{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Main window: engine commands + event stream",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
